import { AnotherDemoHighLightDirective } from './another-demo-high-light.directive';

describe('AnotherDemoHighLightDirective', () => {
  it('should create an instance', () => {
    const directive = new AnotherDemoHighLightDirective();
    expect(directive).toBeTruthy();
  });
});
