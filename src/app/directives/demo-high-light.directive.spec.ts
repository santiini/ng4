import { DemoHighLightDirective } from './demo-high-light.directive';

describe('DemoHighLightDirective', () => {
  it('should create an instance', () => {
    const directive = new DemoHighLightDirective();
    expect(directive).toBeTruthy();
  });
});
